<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjl.mycloud.mapper.ReportingMapper">
    <select id="getById" resultType="com.mjl.mycloud.dto.MPRReporting">
        select * from mpr_reporting where mpr_reporting_id = #{id} ;
    </select>
    <!--<insert id="addUser" parameterType="org.sang.mybatis.model.User">-->
        <!--insert into user (username,address) values (#{username},#{address});-->
    <!--</insert>-->
    <!--<update id="updateUserById" parameterType="org.sang.mybatis.model.User">-->
        <!--update user set username=#{username},address=#{address} where id=#{id}-->
    <!--</update>-->
    <!--<delete id="deleteUserById">-->
        <!--delete from user where id=#{id}-->
    <!--</delete>-->
</mapper>
